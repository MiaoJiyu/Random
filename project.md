## 项目总结与新增需求规格书

### 一、项目概述
- **当前状态**：已实现一个基于 Python + Tkinter 的桌面端加权随机数生成器（GUI 程序）。
- **核心功能**：支持对数字集合及其权重进行灵活编辑，按权重随机抽取一个数字，并以动画效果展示抽取过程。
- **数据持久化**：支持导入/导出 JSON 配置文件。
- **适用场景**：抽奖、决策、概率模拟等。

---

### 二、现有功能清单（已实现）

#### 1. 随机数范围管理
- 单个添加/删除数字
- 批量添加（区间批量，如 1~100；支持正则表达式匹配模式）
- 批量删除（多选删除、区间删除）
- 所有数字自动赋予默认权重（批量添加时可统一设置权重）

#### 2. 权重管理
- 编辑单个数字权重（双击列表项或输入数字+权重）
- 批量编辑权重（多选后统一设置权重，或一键设置全部数字为统一权重）

#### 3. 随机数生成（核心交互）
- 根据权重预选一个目标数字（但不立即显示）
- 动画效果：界面上随机闪动数字（持续 5 秒或用户点击“停止”）
- 动画结束后显示预选的目标数字（实现“预定结果”）

#### 4. 数据配置管理
- 导出当前所有数字及权重为 JSON 文件
- 导入 JSON 文件覆盖当前配置
- 清空所有数据

#### 5. 界面与交互
- 左右分栏布局：左侧列表展示数字、权重、概率，右侧集中控制面板
- 统计信息区：显示总数、总权重、平均概率
- 自适应分辨率（窗口大小随屏幕调整，右侧面板可滚动）

---

### 三、新增需求（待开发）

#### 1. 多样化抽取方式（视觉增强）
- **纯数字模式**：保持现有数字闪动动画。
- **大转盘模式**：增加一个模拟转盘（轮盘）的图形界面，转盘旋转后停止在预选数字上。
- **要求**：实际抽取结果仍由加权算法提前决定，转盘仅作为视觉表现（增强刺激性）。
- **用户可切换**两种模式。

#### 2. 数据导入增强（CSV / Excel）
- 支持从 **CSV** 或 **Excel (.xlsx)** 文件导入数字及权重。
- **模板要求**：第一列为数字，第二列为权重（可选，若缺失则默认权重为 1）。
- 导入时提供示例模板下载功能。

#### 3. 历史记录与统计图表
- **历史记录**：记录每次生成的结果（时间、数字、权重），可查看历史列表。
- **统计图表**：以图表（如柱状图、饼图）展示历史抽取结果的分布，帮助分析随机性。
- 技术建议：可使用 `matplotlib` 或 `plotly` 绘制图表。

#### 4. 双端支持（网页 + 客户端）
- **客户端版本**：继续完善当前的桌面程序，增加上述新功能。
- **网页版本**：提供一个轻量级 Web 应用，支持同样的核心功能（编辑数字/权重、抽取动画、历史查看）。
- **云端配置同步**：允许用户通过网页版配置项目（数字、权重、抽取模式），并同步到客户端（或反之）。实现方案可选用 REST API + 本地存储。

#### 5. 云端配置管理
- 项目配置（数字、权重、抽取方式）存储在云端（如服务器或云数据库）。
- 用户可通过网页登录（可选）管理和分享配置。
- 客户端可从云端拉取配置，也可将本地配置上传至云端。

---

### 四、技术要求与建议

- **客户端**：继续使用 Python + Tkinter（或可考虑 PyQt 以获得更丰富的图形组件，如转盘）。
- **网页端**：推荐使用 Flask/Django 后端 + 前端（HTML/CSS/JS，可选用 Vue/React）。
- **数据库**：SQLite 或 PostgreSQL 存储历史记录、用户配置等。
- **图表库**：`matplotlib`（客户端）、`ECharts`（网页端）。
- **数据导入**：使用 `pandas` 处理 CSV/Excel。
- **动画实现**：客户端可用 `tkinter.Canvas` 或 `PIL` 绘制转盘；网页端用 Canvas 或 CSS 动画。

---

### 五、开发优先级建议

1. **扩展数据导入（CSV/Excel）** – 基础功能增强，易于实现。
2. **增加转盘模式** – 提升用户体验，可先做客户端版本。
3. **历史记录与图表** – 增强数据分析能力。
4. **网页端开发** – 可分阶段，先实现基本网页版，再添加云端同步。

---

### 六、交付物期望

- 完整的客户端程序（含所有新增功能）
- 可独立运行的网页版应用（或 Docker 部署）
- 用户手册/操作说明
- 数据库设计文档（如有）
- 源代码及注释
